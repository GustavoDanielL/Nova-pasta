# Frontend (Electron + Tailwind) — README

Quick start:

```bash
cd frontend
npm install
npm run start
```

To build distributables (requires electron-builder):

```bash
chmod +x build_electron.sh
./build_electron.sh
```

The app expects the backend API at `http://127.0.0.1:8000`.
